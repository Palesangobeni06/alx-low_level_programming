#include <stdio.h>
/**
 * main - Entry point
 * Return: (0)
 */

int main(void)
{
	char c;
	
	int e;

	c = 'a';
	e = 0;
	while
		(e < 10) {
			putchar(e + '0');
			e++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
